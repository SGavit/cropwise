# CropWise Verification Notes

- The crop-photo interface was visually checked at desktop and mobile breakpoints.
- A compressed 115,309-byte JPEG test image was successfully loaded into the browser upload control.
- The preview session currently reports no signed-in CropWise user through `auth.me`; the protected request should redirect to the existing OAuth sign-in flow.
- The unsigned crop-photo action now displays “Sign in to identify crop” instead of attempting an unauthenticated analysis request.
- The first sign-in click followed a hot reload that cleared the selected test photo, leaving the disabled action unable to invoke the login handoff; a fresh upload is required for the browser navigation check.
- With a fresh crop photo selected, the unsigned action correctly routes to the Manus OAuth sign-in page instead of producing a JSON parsing error.
- Automated release verification passed: 5 tests, TypeScript type checking, and the production build all completed successfully.
- Anonymous quick-scan verification: a compressed 90,775-byte crop-leaf image was selected in the unsigned browser session, and the dashboard showed the no-storage disclosure before submitting the public quick-scan request.
- After the parser fix refreshed the form, the same compact leaf image was reselected through the page’s file-selection handler for a second live quick-scan attempt.
- The repaired anonymous quick-scan request was submitted successfully from the unsigned browser session and entered its visible analysis state.
- Live anonymous quick scan succeeded: the result panel identified the submitted botanical leaf illustration as a likely tobacco crop at moderate confidence, listed visible observations, noted the yellow-halo lesion uncertainty, supplied field-photo follow-up steps, and repeated the agronomist-review and no-storage safeguards.
- Objective browser extraction confirmed the populated result text: “Tobacco (likely)”, “Moderate” confidence, visible observations, suggested next steps, agronomist-review guidance, and “Quick scan does not save your photo or attach it to a CropWise account.”
- Desktop and mobile visual checks after the interaction enhancement confirm that the dashboard, scan upload card, navigation, and responsive stacked layout render without visible overlap or cropped controls. The new result actions are conditional on a completed scan and were not present in this empty-state capture.
- A 899,290-byte CropWise leaf image was loaded into the anonymous quick-scan form in preparation for live verification of the new progress, sharing, and PDF controls.
- The first binary quick-scan transport was rejected by the external gateway with HTTP 403 and an HTML response, even though the local server route was registered. The client now presents a clear retry message rather than attempting to parse the HTML as JSON.
- The gateway accepts a small application-octet-stream request and returns JSON validation feedback, so CropWise now sends the compressed photo as generic binary while supplying the validated image type in a separate request header.
- The 899,290-byte CropWise leaf image was reselected after the transport update; the browser prepared it before submission.
- The compressed octet-stream request was also rejected with an external HTTP 403 before reaching the local server, indicating that the gateway is enforcing a request-size threshold rather than filtering only the image MIME type.
- The rejected prepared payload measured approximately 318,864 bytes. CropWise now reduces anonymous quick-scan photos to 88 KiB or below before submission to stay within the observed gateway envelope.
- Crop-type market controls were verified interactively on desktop: the editor loaded distinct Maize and Groundnut figures, saved a Groundnut price of ₹3,450, and the market pulse updated after Groundnut was selected from the shortlist.
- A real mobile-browser pass at 375 × 812 verified the crop-type market editor: Soybean loaded its own ₹4,100 and ₹90 draft values, saved a revised ₹4,250 price, updated the Soybean market pulse, and preserved ₹4,250 after a later edit was cancelled.
- Manual farm location was verified in real browser sessions at 1280 × 720 and 375 × 812. Entering Ashta, Sehore, and Madhya Pradesh updated the farm-location control after saving. The mobile selector remains available as a compact icon-and-chevron control, and the form modal scrolls so all fields and the save action remain reachable.
- In isolated signed-in browser checks, successful geolocation activation changed the location control to “Current GPS location” at 1280 × 720 and 375 × 812. Simulated denied and unavailable geolocation responses retained the manual fallback form and saved Ashta, Sehore, Madhya Pradesh successfully.
- The expanded signed-in browser pass explicitly asserted the session-only privacy text (“not saved to your account”). It confirmed successful current-location activation at both desktop and mobile sizes and confirmed denied and unavailable fallbacks preserved manual village, district, and state entry at both sizes.
- Dashboard action verification at 1280 × 720 and 375 × 812 confirmed the analysis-progress status, populated scan result, share-link creation and copy fallback, and PDF download. The desktop pass also opened and closed the full field-brief panel successfully.
- A compact 5,455-byte crop image passed through the live local anonymous `/api/quick-scan` transport with HTTP 200 and returned a structured no-storage crop-analysis payload, confirming the earlier response-format failure is resolved within the gateway-safe request envelope.
- In isolated signed-in browser checks at desktop and mobile sizes, the account menu displayed the sign-out confirmation, the cancel path left the session active, and confirmed sign-out showed the completion toast before the menu switched to the guest sign-in action.
- The district weather card loaded live conditions and a four-day forecast for Sehore, Madhya Pradesh. The mandi card clearly reported a temporary provider-unavailable state instead of mixing unavailable data with a live price.
- The language selector changed the core dashboard labels, weather and market controls, account-profile labels, and accessible selector name to Hindi and Marathi. Hindi displayed “आज आपके खेत में” and Marathi displayed “आज तुमच्या शेतात”; both retained the visible weather-refresh and market-value actions.
- A signed-in dashboard refresh completed with the farm profile absent and no browser-console errors. The protected profile query now returns `null` instead of `undefined`, and the regression test covers that profileless authenticated state.
- Private farm records were verified at 1280 × 720 and 375 × 812 with an isolated signed-in browser: saving Ashta, Sehore, Madhya Pradesh and crop preferences invoked the protected profile mutation, and the account history panel displayed only the signed-in farmer’s saved Maize/Makka scan. A router regression test also confirms that every successful private crop scan writes a user-scoped history record.
- Responsive browser checks at 1280 × 720 and 375 × 812 verified English, Hindi, and Marathi switching and a farmer-initiated weather refresh. The refreshed Sehore card displayed the expected 28° and “Partly cloudy” weather result at both sizes.
- Hindi and Marathi location-flow checks at 1280 × 720 and 375 × 812 confirmed translated current-location prompts, session-only privacy notices, manual-location actions, and village, district, and state form labels. The English, Hindi, and Marathi catalogue now also covers the core header, weather, market, profile, history, and common action labels.
- Full-route language coverage now uses a shared app-level language provider. The dashboard, public shared-scan page (including loading and unavailable-link states), not-found page, menus, dialogs, image-upload feedback, scan-result controls, market editor, sign-out confirmation, and global recovery handoff all use the same English, Hindi, and Marathi selection.
- Desktop screenshot review at 1280 × 720 confirmed that the expanded dashboard and the unavailable shared-scan recovery page render cleanly. A full-page mobile review at 375 × 812 confirmed the stacked dashboard layout remains readable with the expanded translated labels and controls.
- The CEDA Agmarknet adapter retains the documented `Authorization: Bearer <token>` request header. Live credentials require organization-level access, so the provider-specific credential test has been removed from the default regression suite and the dashboard retains its safe mandi-unavailable fallback.
- New crop scans now send the selected language to the vision workflow and return farmer-facing narratives in English, Hindi, or Marathi. Existing shared scans transparently display a localized notice when their stored narrative language differs from the current selection.
- Live weather provider codes are mapped to the selected-language condition labels rather than displaying provider English. The selected language is also sent to the anonymous binary quick-scan route.
- Post-patch multilingual verification passed: 30 tests across 12 files and a production build succeeded. The final active-route source audit covers the dashboard, shared scan, not-found, app shell, and global recovery handoff; remaining literal strings belong only to unused template/showcase components or internal error construction.
- Weather-unavailable regression verification now returns a typed Open-Meteo unavailable state rather than throwing through tRPC. The dashboard preserves its in-card loading/retry presentation while the provider request is pending; the server regression test confirms the resolved unavailable payload and the complete suite passes.
- Post-fix browser verification at 19:24 IST: a farmer-initiated Hindi weather refresh resolved to the localized Sehore live-weather card, and the fresh browser-console capture contained no API query errors. The typed unavailable branch remains unit-covered for provider timeouts, so a future provider outage will render the same in-card retry state instead of rejecting the query.
- Final weather-fallback proof: the Open-Meteo timeout test returns the typed `status: "unavailable"` payload, the dashboard source regression asserts its localized `weatherUnavailable` in-card branch and confirms no `weatherQuery.isError` fallback, and the post-fix browser refresh did not emit a new API query error.
- Weather experience enhancement verification: the desktop Farm location dialog renders the searchable **Search a forecast location** panel with translated instructional copy and an accessible search input. The phone-sized weather card renders stable animated placeholder geometry while weather data is fetching, without shifting the dashboard layout. The complete suite passes 33 tests across 12 files, and TypeScript checking plus the production build complete successfully.
- Desktop interactive check: the Hindi Farm location dialog opened with translated heading, privacy disclosure, forecast-search instructions, and query input. The existing forecast choices render as selectable localized cards while the active Sehore weather card remains visible behind the dialog.
- Phone-width check at 375 × 812: the dashboard weather card rendered its structured loading skeleton without overflow, overlap, or layout shift. The desktop Hindi search interaction, server search tests, and all-language interface regression tests jointly verify the searchable manual selection and translated state coverage.
- Final live search interaction: with Hindi selected, entering `Sehore` in the forecast-location input immediately showed the translated search-loading message **पूर्वानुमान स्थान खोजे जा रहे हैं…** while preserving selectable location results and the active weather card.
- Final location-selection verification: selecting **Sehore, Madhya Pradesh** updated the dashboard location to **Sehore**, showed the Hindi success notice, and triggered the structured Hindi weather skeleton with **आपके खेत का पूर्वानुमान लाया जा रहा है…**. The skeleton remained contained within the weather card, with no console-query failure or layout overlap.
- Completion check: after the selected Sehore forecast request resolved, the Hindi card displayed localized live weather for **Sehore, Madhya Pradesh** (24°, **पूरा बादल**) and restored the localized refresh control, confirming the search → selection → skeleton → result flow.
- Interactive forecast-location verification: in Hindi, entering `Sehore` returned a translated result list; choosing **Sehore, Madhya Pradesh** updated the dashboard location and showed the localized confirmation. The weather card immediately rendered its animated skeleton and Hindi fetching message while the new forecast request ran. Regression coverage confirms English, Hindi, and Marathi loading, retry, unavailable, and forecast-search state copy.
- Direct weather-component verification: `WeatherExperienceStates.test.tsx` executes rendered checks for the animated skeleton in English, Hindi, and Marathi; forecast-search hint, loading, results, no-results, and unavailable branches; and the result-selection callback. The complete suite passes **37 tests across 13 files**.
- Responsive visual verification: live preview captures at **375 × 812** and **1280 × 720** show the structured weather-card skeleton without horizontal clipping, overlap, or layout shift. The mobile capture keeps paired skeleton bars contained inside the weather card, while the desktop capture preserves the weather and market-card alignment.
- Extended forecast verification: the Open-Meteo adapter requests five forecast days and normalizes daily date, condition code, high, low, and rain-probability values. Direct weather-component tests render five forecast tiles, localized forecast headings, and accessible sunny, cloudy, rainy, storm, fog, and snow visual mappings. The responsive 1280 × 720 and 375 × 812 captures retain the five-column weather skeleton without clipping while the external provider is unavailable in preview.
- Populated extended forecast verification, 22 August 2026: the live Sehore, Madhya Pradesh response rendered the translated Hindi five-day panel immediately below the daily weather, market, and crop-cycle cards. The panel showed five named day tiles, high/low temperatures, rain probabilities, and rain/storm condition visuals, with crop recommendations beginning below the panel.
- Phone-width fallback verification, 22 August 2026: two full-page **375 × 812** preview captures showed the weather request in its bounded animated loading state without horizontal overflow, overlap, or control clipping. The provider did not resolve within those isolated preview captures; the populated desktop capture and direct five-tile component render tests provide the corresponding resolved-forecast evidence, while the mobile capture confirms the safe loading path and responsive container behavior.
- Resolved mobile forecast verification, 22 August 2026: the already-live Sehore weather page was checked at **375 × 812**. The Hindi forecast panel visibly rendered below the daily cards with five normalized forecast tiles, including rain and storm visuals, temperatures, and rain chances. The forecast strip intentionally scrolls horizontally on the narrow viewport (`430 px` scroll width within a `300 px` visible strip), has no page-level horizontal overflow (`360 px` body width within a `375 px` viewport), and exposes a visible scroll indicator; the focused capture is retained at `screenshots/cropwise-mobile-five-day-375x812.png`.
- Expanded Crop Library desktop review, 22 August 2026: the live Hindi Crop Plan view opened a complete Groundnut profile from the Crop Library. The desktop guide presents purpose, climate and timing, field establishment, seed and planting, soil and nutrition, water care, harvest and storage, and field-watch guidance in distinct readable sections, with the local-consultation caution retained at the end. The verifier confirmed six guide sections with no page-level horizontal overflow; the focused desktop capture is retained at `screenshots/cropwise-crop-library-profile-desktop.png`.
- Expanded Crop Library phone review, 22 August 2026: at **375 × 812**, the detailed Groundnut guide stacks the profile heading, sowing window, seasonal water plan, soil fit, crop purpose, climate and timing, and field-establishment guidance into readable cards. The live verifier confirmed all six guide sections remain reachable by ordinary vertical scrolling and found no page-level horizontal overflow (`360 px` document width in a `375 px` viewport); the focused phone capture is retained at `screenshots/cropwise-crop-library-profile-mobile.png`.
- Detailed Crop Library release checks, 22 August 2026: `pnpm test` completed with **50 passing tests across 15 files**, including the data-driven multilingual Crop Library profile assertions. One-shot TypeScript validation completed without errors, and the production build completed successfully after temporarily releasing the TypeScript watcher. The development service was then restarted successfully with no type errors.
- English and Marathi detailed-profile verification, 22 August 2026: the live desktop Crop Library rendered the Groundnut guide in English, visibly covering the sowing window, water plan, soil fit, purpose, climate and timing, field establishment, seed and planting, soil and nutrition, water care, harvest and storage, and field watch. The phone-sized Marathi guide rendered the same profile hierarchy, including **पिकाचा उपयोग**, with all six sections reachable by vertical scrolling and no horizontal page overflow. Focused captures are retained at `screenshots/cropwise-crop-library-profile-english-desktop.png` and `screenshots/cropwise-crop-library-profile-marathi-mobile.png`.
- Additional locale check, 22 August 2026: the focused **375 × 812** English profile capture clearly shows Groundnut’s sowing window, `100%` water-planning reference, suitable soils, crop purpose, and climate-and-timing guidance without horizontal clipping. The first Marathi desktop capture confirmed the Crop Plan workspace but was positioned above the expanded profile, so a focused Marathi guide capture remains required before release.
- Focused Marathi desktop guide, 22 August 2026: the corrected desktop capture now frames the Groundnut profile directly. It visibly shows **पिकाचा उपयोग**, climate and timing, field establishment, seed and planting, soil and nutrition, water management, harvest and storage, and field watch, alongside the sowing window, `100%` water-planning reference, and suitable soil guidance. The browser verifier found six guide sections and no page-level horizontal overflow; the focused capture is retained at `screenshots/cropwise-crop-library-profile-marathi-desktop.png`.
- Final release checks, 22 August 2026: `pnpm test`, `pnpm check`, and `pnpm build` completed successfully. Vitest reported **41 passing tests across 13 files**; the weather-provider timeout messages are expected simulated fallback-test output. TypeScript reported no errors and the production bundle completed successfully.
- Workspace and Krishi Expert verification, 22 August 2026: the overview now renders the current localized date (`Saturday, 22 August 2026` in English). The live Hindi Crop Wealth Watch workspace showed three selectable crop chips, three practical value-signal cards, refresh and value-change controls, and no desktop page overflow. The live Hindi Krishi Expert workspace showed contextual safety copy, a usable textarea and suggested prompts; a submitted sowing question produced one completed assistant reply and preserved the user question. At **375 × 812**, both workspace views rendered their expected cards/chat input without page-level horizontal overflow (`360 px` body width within a `375 px` viewport).
- Focused phone screenshot review, 22 August 2026: Crop Wealth Watch retains its green value-signal header, horizontally reachable crop choices, price and refresh controls, and stacked signal cards at **375 × 812**. Krishi Expert retains its local-context safety card, clear chat-empty-state prompt, and suggested-question controls at the same size; the page scroll remains intentional and no controls are clipped at either viewport edge.
- Final workspace release checks, 22 August 2026: the complete suite reported **45 passing tests across 14 files**, including three deterministic Krishi Expert procedure tests. One-shot TypeScript validation completed without errors, and the production build completed successfully after stopping a redundant TypeScript watcher that had previously exhausted the sandbox build memory. The expected weather-timeout messages remain confined to simulated fallback tests.
- Four-tab navigation verification, 22 August 2026: a live browser interaction pass explicitly activated every workspace tab. Overview returned to the localized dashboard hero and current date; Crop Plan opened its dedicated localized planning view with crop selection, suitability/yield/profit details, soil-preparation guidance, and a weather-preparation card; Crop Wealth Watch opened its three crop-specific market, rain, and field-value signals; and Krishi Expert opened its contextual chat interface. The completed live chat answer was observed after allowing the provider up to 60 seconds to respond.
- Crop-planning feature verification, 22 August 2026: the live Hindi Crop Plan workspace renders its selected-crop planning cards, localized calendar reminder panel, sign-in-safe reminder form, and crop-library reference surface. The desktop crop library presents all three starter crops with sowing windows, seasonal water-planning percentages, and soil guidance. The separate Crop Wealth Watch capture renders the per-crop market, rain, and field-value signals alongside a threshold-settings preview and the expanded three-field form for price floor, rain chance, and soil-moisture minimum. Both desktop layouts remain within the page width and retain visible, keyboard-native controls.
- Crop-library visual review, 22 August 2026: the focused desktop library view shows the featured Maize guidance in a three-column summary and the Maize, Groundnut, and Soybean comparison cards beneath it. The focused **375 × 812** Hindi view keeps the library heading, featured crop, sowing window, `98%` seasonal water-planning reference, and suitable soil guidance visible in a single-column card without horizontal clipping; additional crop cards remain reachable by ordinary vertical scrolling.
- Final crop-planning release checks, 22 August 2026: the complete suite reported **49 passing tests across 15 files**, including protected reminder and threshold persistence coverage. One-shot TypeScript validation completed without errors, and the production build completed successfully after temporarily releasing redundant local watch processes; the development service was then restarted successfully. The expected weather timeout messages remain limited to the simulated weather-fallback tests.
- Focused mobile alert-settings verification, 22 August 2026: at **375 × 812**, Crop Wealth Watch opens the dedicated threshold form with all three crop-specific controls—minimum price, maximum rain chance, and minimum soil-moisture reference—visible and reachable after normal vertical scrolling. The form retains its save action, has no page-level horizontal overflow (`360 px` document width in a `375 px` viewport), and leaves the existing threshold preview readable directly above it.
